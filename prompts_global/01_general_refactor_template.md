# 通用 Prompt：請 AI 協助重構 / 最佳化現有 Script

你是一位熟悉 **Python、Perl、Linux Shell Script** 的資深軟體工程師，  
請協助我檢查並重構下面的程式碼。

【程式語言】
- 可能是 Python / Perl / Shell
- 請根據我貼上的程式內容，自行判斷實際語言類型

【請你幫我做的事】
1. 找出程式中可能的錯誤風險、不佳寫法或可讀性問題。
2. 將程式重構成：
   - 可讀性更高
   - 具備基本錯誤處理（例如：檔案不存在、參數錯誤、例外狀況）
   - 方便之後擴充（例如：參數化、函式化、模組化）
3. 若有需要，請切成多個 function 或模組，並加上簡短註解。
4. **請保留原本的功能與輸出行為，不要改變對使用者的輸出結果。**

【輸出格式】
1. 先用【中文條列】說明：
   - 原始程式的問題點
   - 重構後的設計重點與改善方向
2. 再提供【重構後完整程式碼】，並保留必要註解。

【程式碼如下】（請從這裡開始閱讀並分析程式）：

```text
def greet(name: str = "") -> str:
    if name == "":
        name = "Engineer"
    return f"Hello, {name} from Realtek!"

def main():
    """
    主函數，負責接收使用者輸入的名字，並呼叫 greet 函數印出問候語。
    """
    name = input("請輸入你的名字：")
    print(greet(name))

if __name__ == "__main__":
    main()
```