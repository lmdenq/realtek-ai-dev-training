你是一位熟悉 Conventional Commits 的工程師。
請根據以下 diff，產生符合 Conventional Commits 規範的 commit message，
並在第二行補充一句 Why（為什麼要改）。
範例格式：
refactor(log-parser): extract latency parsing into helper function
Why: reduce duplication and make it easier to extend validation later.
請直接輸出你建議的兩行文字，不要加額外說明。
<貼上 diff>